{
  "id": "instagram",
  "kind": "integration",
  "category": "social",
  "transport": "native",
  "version": "1.0.0",
  "name": "Instagram",
  "vendor": "Meta",
  "description": "Read the media posted by your Instagram Business account.",
  "intentHints": ["read our instagram posts", "instagram media"],
  "authType": "oauth2",
  "providedTools": [
    { "id": "instagram.fetch_media", "description": "Read the connected account's own media.", "input": { "ig_account_id": { "type": "string", "required": false }, "limit": { "type": "number", "required": false } }, "output": { "media": { "type": "array", "required": true } } }
  ]
}
