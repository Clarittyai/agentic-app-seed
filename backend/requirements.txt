# Clarity Backend Requirements

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
# Pin websockets to a range uvicorn 0.27 supports — unpinned it resolves to a
# 14+/16 release whose API uvicorn 0.27 can't use, logging "No supported
# WebSocket library detected" on every upgrade request.
websockets>=10.4,<12
python-multipart==0.0.9

# Clarity SDK (from PyPI)
# For local development: pip install -e /path/to/claritty-sdk
# Floor pinned to 1.2.0: only >=1.0.2 ships the `claritty_sdk` (two-t) module —
# 1.0.0 shipped `clarity_sdk` (one t). A stale pip Docker-build cache that
# resolved the old version made `import claritty_sdk` fail the boot check.
claritty-sdk>=1.2.0,<2.0.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# AI & LangChain
langchain==0.1.6
anthropic==0.18.1
langchain-anthropic==0.1.4

# Scheduling
apscheduler==3.10.4
pytz==2024.1

# HTTP Client
httpx==0.26.0

# Utilities
python-dotenv==1.0.1
